using System;
using System.Collections.Generic;
using System.Text;

namespace OpenClose
{
    public class MergeSorter
    {
        public List<int> Sort(List<int> list) 
        {
            Console.WriteLine
                ("Merge is good but not as fast as quicksort 0(n * Log(n))");
            return list;
        }
    }
}
